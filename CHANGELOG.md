# 变更记录

## v8

- 修复微信小程序广告页误点右上角关闭键、导致直接退出小程序的问题。
- 删除泛化 `ImageView` 结构关闭规则。
- 小程序开屏广告只点击明确的“跳过/跳過”文本。
- 小程序全屏/插屏广告规则改为默认关闭，仅匹配“关闭广告/直接关闭”等明确控件。
- 激励视频结束后关闭继续默认关闭，并收紧为明确的奖励完成与关闭广告组合。

## v7

- 重写微信小程序广告规则。
- 覆盖 `AppBrandUI`、`AppBrandUI00-05` 与 `AppBrandLaunchProxyUI`。
- 开屏广告增加 800ms 延迟、允许二次点击，并增加 WebView 根节点兜底选择器。
- 增加小程序全屏/插屏广告关闭规则。
- 增加默认关闭的“激励视频结束后关闭”规则，避免提前关闭导致奖励失效。
- 保留电脑微信登录确认规则并继续默认关闭。

## v6

- 将订阅源码与构建产物同步，避免重新构建后退回模板 v0。
- 修复 GKD 不支持的 `\s` 选择器转义写法。
- 保留用户快照精准规则。
- 汇总 60 余个常见应用的低风险规则。
- 增加开屏广告、弹窗、更新提示、青少年模式全局规则。
- 恢复推送源码后自动构建 `dist` 的 GitHub Actions。

## v5

- 增加常见应用低风险开屏与弹窗规则。

## v4

- 修复英文 Skip 选择器语法错误。

## v3

- 合并用户快照精准规则与公开订阅参考规则。
